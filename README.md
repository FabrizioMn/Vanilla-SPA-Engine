🚀 Vanilla SPA Router & Project Manager
Una Single Page Application (SPA) construida exclusivamente con JavaScript Vanila, sin librerías ni frameworks externos. Este proyecto demuestra el dominio de la manipulación del DOM, la gestión de rutas mediante el History API y la persistencia de datos en el navegador.

🌟 Características Principales
Custom Router: Sistema de navegación dinámica utilizando el History API y eventos popstate.

CRUD Persistente: Creación y eliminación de proyectos con persistencia total mediante localStorage.

Reactividad Manual: Actualización selectiva de la interfaz de usuario (re-renderizado del DOM) sin recargar la página.

Modo Oscuro: Sistema de temas (Light/Dark mode) con memoria de preferencia de usuario.

Consumo de API: Integración inicial con JSONPlaceholder mediante fetch y async/await.

Arquitectura Modular: Separación de responsabilidades en funciones independientes (Formulario, Lista, Lógica de Datos).